name: turf_booking_app
description: "TurfBook - Book sports turfs instantly"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.8
  firebase_storage: ^12.4.8
  google_sign_in: ^6.3.0
  
  # UI
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  
  # Utilities
  intl: ^0.20.2
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
